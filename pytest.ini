[pytest]
addopts = -m "not slow" --durations=10 --strict-markers
markers =
    slow: long-running tests (excluded by default)
    integration: end-to-end or multi-stage integration tests
    opticommpy: tests that require OptiCommPy-backed execution
